spring:
  application:
    name: wms
  datasource:
    url: jdbc:mysql://210.95.64.60:3306/wms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: nbs_dev
    password: nbsdev1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
#  sql:
#    init:
#      mode: always
  cache:
    type: redis
    redis:
      time-to-live: 600000  # 10분
      cache-null-values: false
  data:
    redis:
      host: 210.95.64.60
      port: 6379
      database: 0

# 서버 설정
server:
  port: 8888
  address: 0.0.0.0
#  servlet:
#    context-path: /api

# 로깅 설정
logging:
  level:
    root: INFO
    com.wms: DEBUG
    org.hibernate.SQL: DEBUG

# swagger
springdoc:
  swagger-ui:
    path: /docs
